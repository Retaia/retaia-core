## Summary
- One clear objective for this PR.

## Out Of Scope
- Explicitly list what this PR does not do.

## Specs Impact
- Specs update required: `yes` / `no`
- If `yes`, link the `retaia-docs` PR/commit and summarize contract/runtime impact.
- If `no`, state why behavior remains compliant with current specs.

## Risks
- Main technical and runtime risks introduced by this change.

## Rollback
- Exact rollback strategy if deployment/regression fails.

## Tests
- List executed checks (unit/integration/contract/e2e as relevant).
- Include non-regression tests added for bug fixes.

## Notes
- Optional additional context for reviewers.
